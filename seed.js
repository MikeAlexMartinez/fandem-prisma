require("dotenv-safe").config();
const db = require("./scripts/seed-db");

const setup = async () => {
  // add teams
  // add countries
  // add subscription types
  // add user roles
  // add super user (ME)
};

setup();
